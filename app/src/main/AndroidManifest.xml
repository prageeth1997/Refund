<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.refund">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".cUserProf" />
        <activity android:name=".cCard_Payment" />
        <activity android:name=".cDetail_Form" />

        <activity android:name=".viewRefund"></activity>
        <activity android:name=".refundUpdate" />
        <activity android:name=".refund" />
        <activity android:name=".myorders">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>